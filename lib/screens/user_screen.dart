import 'package:flutter/material.dart';
class UserScreem extends StatelessWidget{
  const UserScreem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PANTALLA DE USUARIO'),
      ),
    );
  }

}